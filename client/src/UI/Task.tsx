import { Box, Typography } from "@mui/material";

const Task = () => {
  return (
    <Box sx={{padding: "1.2rem 1rem" }}>
      <Typography>TODO this</Typography>
    </Box>
  );
};

export default Task;
